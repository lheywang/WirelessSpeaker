# ================================================================================
# RELEASER
# ================================================================================
name: releaser
run-name: Generate a release when push on the main branch !
# --------------------------------------------------------------------------------
on:
  push:
    branches: [main]
# --------------------------------------------------------------------------------
jobs:
  job_build:
    uses: ./.github/workflows/builder.yml
  # - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
  job_doc:
    uses: ./.github/workflows/doc.yml
  # - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
  releaser:
    runs-on: ubuntu-latest
    needs: [job_build, job_doc] # Require the executable and the documentation to be built

    steps:
      - name: Test
        run: |
          echo "I'm running merger !"
