# ================================================================================
# TESTER
# ================================================================================
name: tester
run-name: Perform unittests on the source code !
# --------------------------------------------------------------------------------
on: workflow_calls
# --------------------------------------------------------------------------------
jobs:
  dep1:
    uses: ./.github/workflows/builds/docker.yml@tests
  # - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
  dep2:
    uses: ./.github/workflows/formats/cpp.yml@tests
  # - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
  dep3:
    uses: ./.github/workflows/formats/py.yml@tests
  # - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

  testing:
    runs-on: ubuntu-latest
    needs: [dep1, dep2, dep3] # Require the code to be formatted AND the docker image to be ready

    steps:
      - name: Test
        run: |
          echo "I'm running tests !"
